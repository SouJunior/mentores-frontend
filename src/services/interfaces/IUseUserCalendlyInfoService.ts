export interface ICalendlyUserInfo {
    id?: string;
    calendlyUserUuid?: string; 
    calendlyAccessToken?: string; 
    calendlyRefreshToken?: string; 
    accessTokenExpiration?: Date; 
    calendlyName?: string; 
    agendaName?: string; 
    mentorId?: string; 
  }